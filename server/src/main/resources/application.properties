#MySQL Properties
spring.datasource.url=jdbc:mysql://localhost:3306/vttpfinalproject
spring.datasource.username=vttpfinalproject
spring.datasource.password=vttpfinalproject

# MongoDB Properties
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=vttpfinalproject
spring.data.mongodb.authentication-database=admin

# Stripe Properties
spring.stripe.apikey=${STRIPE_SECRET}
spring.stripe.clienturl=http://localhost:4200

# OAuth2 Properties
# spring.security.oauth2.client.registration.github.clientId: ${GITHUB_CLIENT_ID}
# spring.security.oauth2.client.registration.github.clientSecret: ${GITHUB_CLIENT_SECRET}
# spring.security.oauth2.client.registration.google.clientId: ${GOOGLE_CLIENT_ID}
# spring.security.oauth2.client.registration.google.clientSecret: ${GOOGLE_CLIENT_SECRET}